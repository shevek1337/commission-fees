export enum CurrencyTypeEnum {
  EUR = "EUR",
}

export enum UserTypeEnum {
  natural = "natural",
  juridical = "juridical",
}

export enum OperationTypeEnum {
  cash_in = "cash_in",
  cash_out = "cash_out",
}
